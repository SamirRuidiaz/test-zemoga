import React from 'react';
import PropTypes from 'prop-types';
import styles from './BoxCounted.module.scss';

const BoxCounted = () => (
  <div className={styles.BoxCounted}>
    BoxCounted Component
  </div>
);

BoxCounted.propTypes = {};

BoxCounted.defaultProps = {};

export default BoxCounted;
